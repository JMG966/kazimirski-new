const React = require('react');

const Acknowledgements = () => {
    return (
        <div>
            <h1>Acknowledgements</h1>
            <p>Add acknowledgements here.</p>
        </div>
    );
};

module.exports = Acknowledgements;
